include _views/post
extends _views/main

block main
  -page.posts.each(function(post){
    +post(post, 'index')
  -})

block head
  title
    = config.title
  meta(name="keywords", content="Bubblings,web前端,css,javascript,nodejs")
  meta(name="description", content=config.title)